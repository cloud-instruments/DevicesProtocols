Double click on ServerClientTest-MULTI.bat to start a Client-Server emulation of CIUP protocol
logs are generated inside the log folder

Any of the settings (timings, channels count, etc) are at the beginning of the bat itself